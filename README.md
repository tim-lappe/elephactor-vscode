# Elephactor VS Code Extension

This repository contains the built Elephactor VS Code extension mirrored from `tim-lappe/elephactor-dev`.

Install the bundled VSIX:

```bash
code --install-extension elephactor-vscode.vsix
```

The unpacked extension is also available in this repository through `package.json` and `dist/`.
